# Warrant — Screenshot Notes And Captions

Use these captions for submission images. Each caption is intentionally short and proof-oriented.

## Screenshot 1 — Scenario sequence + thesis

Caption:
`Warrant starts with one parent authorization, then walks through delegated child actions, denial, approval, and revocation in a single investor-update flow.`

What to capture:

- `/demo` top area with scenario prompt
- sequence strip visible

## Screenshot 2 — Delegation graph

Caption:
`The graph makes delegated authority legible: Planner issues narrower Calendar and Comms child warrants with distinct branch states.`

What to capture:

- graph with Planner, Calendar, Comms nodes
- node statuses visible
- node detail panel open for Comms

## Screenshot 3 — Policy denial

Caption:
`Comms tries an out-of-bounds recipient; Warrant blocks it as a policy denial before provider execution.`

What to capture:

- proof card showing denial
- timeline row for blocked overreach (policy code visible)

## Screenshot 4 — Approval-gated send

Caption:
`Even an in-bounds send is paused for explicit human approval with exact message preview and blast radius.`

What to capture:

- sensitive action approval section
- exact action preview and approval reason

## Screenshot 5 — Branch revoke outcome

Caption:
`Revoking the Comms branch immediately removes downstream authority; later Comms send attempts are blocked while Calendar remains active.`

What to capture:

- `comms-revoked` preset
- revocation block proof card
- timeline rows for revoke + blocked post-revoke send

